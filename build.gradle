// gradle로 task를 수행할 때에 사용되는 설정. 컴파일과는 무관하다.
buildscript {
    ext {
        springBootVersion = '2.4.5'
    }
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:${springBootVersion}")
        classpath "io.spring.gradle:dependency-management-plugin:1.0.11.RELEASE"
    }
}

//모든 프로젝트 공통 설정
allprojects {
    group 'org.example'
    version '1.0-SNAPSHOT'
}

// 서브 프로젝트에만 적용
subprojects {
    apply plugin: 'java'
    apply plugin: 'org.springframwork.boot'
    apply plugin: 'io.spring.dependency-management'

    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }

    // 모든 모듈에서 사용하는 라이브러리
    dependencies {

    }
}

//common 모듈에만 필요한 라이브러리 관리
project(':module-common') {
    dependencies {

    }
}

//user 모듈에만 필요한 라이브러리 관리
project(':module-user') {
    dependencies {

    }
}


//board 모듈에만 필요한 라이브러리 관리
project(':module-board') {
    dependencies {

    }
}